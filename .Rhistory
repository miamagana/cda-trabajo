setwd("DiscoW/CDA/cda-trabajo/")
ls
dir()
source("weather_data.R")
source("read_car_data.R")
spain
library(data.table)
t_mtcars <- transpose(spain)
t_mtcars
colnames(t_mtcars) <- rownames(spain)
data("spain")
data(spain)
rownames(t_mtcars) <- colnames(spain)
t_mtcars
names(t_mtcars)
